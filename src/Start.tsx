import { useEffect, useState } from "react"

const Start = ()  => {


    const clickHandler = () => {
        // setMyNumber(myNumber + 1);
    }
    
    return <button
    onClick={clickHandler}>
        Start
        </button>
}

export default Start